# SibKlimat

Единая точка входа находится в корне проекта.

```bash
cp .env.example .env
npm run dev
```

- сайт: `http://localhost:3000`
- Strapi: `http://localhost:1337/admin`

Все команды для разработки, сборки и деплоя описаны в `DEV.md`.

Главная локальная сборка:

```bash
npm run build
```
